tags:
  ffi:
    test_on: vm
  wasm:
    test_on: browser

platforms: [vm]

override_platforms:
  firefox:
    settings:
      arguments: "-headless"

presets:
  full:
    platforms: [vm, chrome, firefox]
  web:
    platforms: [chrome, firefox]
