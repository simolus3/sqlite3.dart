name: sqlite3
description: sqlite3 bindings via dart:ffi
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  collection: ^1.14.13
  ffi: ^0.1.3
  meta: ^1.1.8

dev_dependencies:
  extra_pedantic: ^1.2.0
  test: ^1.14.4
  build_runner: ^1.10.0
  path: ^1.7.0
  build_ffi_generator:
    git:
      url: https://github.com/simolus3/build_ffi_generator.git