name: custom_extension
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  sqlite3:

  code_assets: ^0.19.7
  hooks: ^0.20.1
  http: ^1.5.0
  tar: ^2.0.0

dev_dependencies:
  test: ^1.26.0
