name: sqlcipher_flutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  sqlcipher_flutter_libs:
    path: ../../sqlcipher_flutter_libs
  flutter:
    sdk: flutter

dev_dependencies:
  sqlite3:
    path: ../../sqlite3
  flutter_driver:
    sdk: flutter
  test: