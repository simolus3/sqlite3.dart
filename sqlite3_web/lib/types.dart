/// Cross-platform types related to the `sqlite3_web` package.
///
/// This library can safely be imported both when targeting the web and when
/// compiling to native targets. Using these types may be convenient for shared
/// options.
library;

export 'src/types.dart';
